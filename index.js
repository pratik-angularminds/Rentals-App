/**
 * @format
 */

import {AppRegistry} from 'react-native';
import {name as appName} from './app.json';
import {AppBinder} from './src/Screens/AppBinder';

AppRegistry.registerComponent(appName, () => AppBinder);
